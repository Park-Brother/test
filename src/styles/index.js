import {StyleSheet} from 'react-native';
import {LIST_BACKGROUND} from "./common";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  listContainer: {
    flex: 1,
    backgroundColor: LIST_BACKGROUND
  }
});